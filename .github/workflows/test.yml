name: Test

on: ['push', 'pull_request']

jobs:
  build:
    runs-on: ubuntu-latest
    container: coajaxial/messaging-mediator
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: composer install